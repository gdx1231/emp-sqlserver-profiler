# emp-sqlserver-profiler 项目目标和运行原理

## 项目目标

### 1. 主要目标
`emp-sqlserver-profiler` 项目的主要目标是提供一个轻量级的、易于使用的 SQL Server 性能分析工具，用于：

- **实时监控**: 实时捕获 SQL Server 数据库的活动和查询执行
- **性能分析**: 分析 SQL 查询的性能指标，如 CPU 使用率、读写次数、执行时间等
- **问题诊断**: 识别慢查询、资源消耗大的操作和潜在的性能瓶颈
- **数据收集**: 收集详细的数据库操作日志，用于后续分析和审计
- **跨平台兼容**: 提供 Java 实现，支持在多种操作系统上运行

### 2. 设计目标
- **易用性**: 提供简单的命令行接口，便于快速部署和使用
- **低侵入性**: 最小化对目标 SQL Server 实例的影响
- **数据持久化**: 将收集的跟踪数据存储在本地数据库中，便于离线分析
- **可扩展性**: 支持多种跟踪事件和自定义过滤条件
- **开源友好**: 采用 MIT 许可证，便于社区贡献和企业使用

## 运行原理

### 1. 整体架构
```
[用户界面/命令行] 
       ↓
[ProfilerControl] ←→ [SqlServerProfiler] 
       ↓                    ↓
[命令解析/控制]      [跟踪管理/状态控制]
       ↓                    ↓
[HSqlDbServer] ←→ [RawTraceReader] ←→ [SQL Server]
    ↓           ↓           ↓
[数据存储] ←→ [事件处理] ←→ [数据读取]
```

### 2. 核心技术原理

#### 2.1 SQL Server 跟踪机制
- 项目利用 SQL Server 的内置跟踪功能 (SQL Trace)
- 通过系统存储过程 `sp_trace_create`, `sp_trace_start`, `sp_trace_stop` 等管理跟踪会话
- 创建轻量级跟踪，仅收集必要的性能数据

#### 2.2 连接建立过程
1. **参数验证**: 验证提供的连接参数 (主机、端口、用户名、密码)
2. **连接测试**: 使用 Microsoft SQL Server JDBC Driver 建立连接
3. **权限检查**: 验证用户是否有创建和管理跟踪的权限
4. **连接池管理**: 使用内部连接池管理与 SQL Server 的连接

#### 2.3 跟踪会话管理
- **创建跟踪**: 调用 `sp_trace_create` 创建新的跟踪会话
- **设置事件**: 配置需要跟踪的事件类型和列
- **启动跟踪**: 调用 `sp_trace_setevent` 和 `sp_trace_start` 启动跟踪
- **状态管理**: 维护跟踪的生命周期 (开始、暂停、停止)

### 3. 数据流处理

#### 3.1 数据采集流程
```
SQL Server → RawTraceReader → ProfilerEvent → ProfilerThread → HSqlDbServer → TRACE_LOG
```

1. **数据读取**: RawTraceReader 从 SQL Server 读取跟踪数据
2. **事件封装**: 将原始数据封装为 ProfilerEvent 对象
3. **异步处理**: ProfilerThread 异步处理事件队列
4. **数据存储**: 将事件数据存储到本地 HSQLDB 的 TRACE_LOG 表

#### 3.2 数据过滤和处理
- **事件过滤**: 根据配置过滤特定类型的事件
- **数据截断**: 对过长的文本数据进行截断以优化存储
- **格式转换**: 将 SQL Server 数据类型转换为适合存储的格式

### 4. 本地存储机制

#### 4.1 HSQLDB 集成
- **嵌入式数据库**: 使用 HSQLDB 作为本地数据存储
- **自动初始化**: 首次运行时自动创建数据库和表结构
- **持久化存储**: 将跟踪数据保存到本地文件系统

#### 4.2 数据表结构
- **TRACE_SERVER**: 存储 SQL Server 连接配置
- **TRACE_LOG**: 存储详细的跟踪记录
- **TRACE_EVENTS**: 定义可用的跟踪事件
- **TRACE_COLUMNS**: 定义跟踪列信息

### 5. 线程模型

#### 5.1 主要线程
- **主线程**: 处理用户输入和命令执行
- **ProfilerThread**: 持续读取和处理跟踪数据
- **定时器线程**: (可选) 定期报告状态信息

#### 5.2 并发处理
- 使用 `ConcurrentLinkedQueue` 管理事件队列
- 确保线程安全的数据处理
- 避免阻塞主控制流程

### 6. 状态管理

#### 6.1 状态类型
- **psStopped**: 跟踪已停止
- **psPaused**: 跟踪已暂停  
- **psProfiling**: 跟踪正在运行

#### 6.2 状态转换
```
psStopped → startProfiling() → psProfiling
psProfiling → pauseProfiling() → psPaused
psPaused → resumeProfiling() → psProfiling
psProfiling/psPaused → stopProfiling() → psStopped
```

### 7. 安全特性

#### 7.1 数据加密
- 使用 AES 加密算法保护敏感信息（如密码）
- 遵循安全最佳实践

#### 7.2 连接安全
- 支持 SSL/TLS 加密连接
- 验证服务器证书（通过 JDBC 参数）

### 8. 性能优化

#### 8.1 存储优化
- 避免使用 BLOB/CLOB 类型减少 .lob 文件大小
- 对大文本字段进行合理截断
- 使用适当的索引提高查询性能

#### 8.2 内存管理
- 使用队列缓冲事件数据
- 避免内存泄漏
- 适时清理无用对象

### 9. 错误处理机制

#### 9.1 连接错误
- 重试机制处理临时连接失败
- 详细的错误信息帮助故障排除

#### 9.2 数据处理错误
- 异常捕获和处理
- 数据完整性保证

### 10. 扩展性设计

#### 10.1 插件架构
- 模块化设计便于功能扩展
- 支持自定义事件处理器

#### 10.2 配置灵活性
- 支持多种跟踪选项
- 可配置的过滤条件

## 应用场景

### 1. 性能调优
- 识别慢查询和高资源消耗操作
- 分析查询执行计划
- 优化数据库性能

### 2. 问题诊断
- 排查数据库性能问题
- 监控异常活动
- 审计数据库访问

### 3. 容量规划
- 分析数据库使用模式
- 预测资源需求
- 规划系统扩展

### 4. 安全审计
- 监控数据库访问
- 记录用户操作
- 检测异常行为

## 与其他工具的比较

| 特性 | emp-sqlserver-profiler | SQL Server Profiler | 其他工具 |
|------|------------------------|---------------------|----------|
| 平台支持 | 跨平台 (Java) | Windows 专用 | 取决于具体工具 |
| 资源占用 | 轻量级 | 较重 | 各异 |
| 部署复杂度 | 简单 (单 JAR) | 需要完整 SQL Server | 各异 |
| 开源 | 是 (MIT) | 否 | 取决于具体工具 |

这个设计使 emp-sqlserver-profiler 成为一个强大而灵活的 SQL Server 监控解决方案，特别适合需要跨平台支持和轻量级部署的场景。